{
	"name": "cdn",
	"version": "0.0.1",
	"private": true,
	"description": "simple image CDN on Cloudflare KV + R2",
	"main": "src/index.ts",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"types": "wrangler types"
	},
	"dependencies": {
		"@hono/zod-validator": "^0.5.0",
		"hono": "^4.9.6"
	},
	"devDependencies": {
		"wrangler": "^4.34.0"
	}
}
